-- Configuration
function love.conf(t)
	t.window.title = "Physics" -- The title of the window the game is in (string)
	--t.version = "0.9.1"         -- The LÖVE version this game was made for (string)
	--t.window.width = 800        
	--t.window.height = 800
	t.console = true
end